export function StatBar({label,value}:{label:string;value:number}){const safe=Math.max(0,Math.min(100,Math.round(value)));return <div className="stat-row"><div className="stat-label"><span>{label}</span><span>{safe}</span></div><div className="stat-track" role="progressbar" aria-label={label} aria-valuemin={0} aria-valuemax={100} aria-valuenow={safe}><div className="stat-fill" style={{width:`${safe}%`}}/></div></div>}
