import type { TimelineEntry } from '../types/game';
const symbols:Record<string,string>={birth:'◉',family:'⌂',school:'▤',relationship:'♡',career:'▣',money:'¤',health:'+',crime:'!',legal:'§',fame:'★',asset:'◆',business:'▦',travel:'✦',achievement:'✹',death:'◇',random:'•'};
export function Timeline({entries}:{entries:TimelineEntry[]}){return <div className="timeline" aria-label="Life timeline">{[...entries].reverse().map(entry=><article className={`timeline-entry importance-${entry.importance}`} key={entry.id}><div className="timeline-age"><span className="timeline-symbol" aria-hidden="true">{symbols[entry.category]??'•'}</span><strong>{entry.age}</strong></div><div className="timeline-copy">{entry.title&&<h3>{entry.title}</h3>}<p>{entry.text}</p>{entry.moneyDelta!==undefined&&entry.moneyDelta!==0&&<small>{entry.moneyDelta>0?'+':''}{Math.round(entry.moneyDelta).toLocaleString()} game currency</small>}{entry.detail&&<details><summary>Details</summary><p>{entry.detail}</p></details>}</div></article>)}</div>}
